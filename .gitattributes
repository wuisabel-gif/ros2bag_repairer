# Keep the project's language stats focused on the actual tool (Python).
# The landing page and in-browser demo under docs/ are documentation/vendored,
# so GitHub Linguist should not count them as the main language.
docs/**            linguist-documentation
docs/**            linguist-vendored
*.html             linguist-documentation
*.html             linguist-vendored
